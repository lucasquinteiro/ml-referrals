# Install into this project's own virtualenv:
#   python3 -m venv .venv
#   .venv/bin/pip install -r requirements.txt
#   .venv/bin/python -m playwright install chromium
playwright>=1.44      # search scraping, screenshots, link builder
httpx
beautifulsoup4        # parsing /ofertas over plain HTTP (no browser)
Pillow                # composing the offer-card image
python-dotenv
openai>=1.0.0        # tweet copy (Groq via the OpenAI-compatible endpoint)
tweepy>=4.14         # official Twitter/X API posting (post_target: twitter_api)
supabase>=2.0.0      # only when store = "supabase"
